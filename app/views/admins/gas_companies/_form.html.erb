<%= form_for [:admins, @gas_company] do |f| %>
  <% if @gas_company.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@gas_company.errors.count, "error") %> prohibited this gas_company from being saved:</h2>

      <ul>
      <% @gas_company.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :logo %><br>
    <%= f.file_field :company_logo %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
